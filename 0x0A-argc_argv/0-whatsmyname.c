#include <stdio.h>

/**
 * main - print 0 argv
 * @argc: unused
 * @argv: array arguments call function
 * Return: Success 0
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
