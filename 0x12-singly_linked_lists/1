#include "lists.h"
#include <stdio.h>
#include <string.h>
#include <stdio.h>

size_t print_list(const list_t *h)
{
	size_t *tmp;

	tmp = h;
	while (h != NULL)
	{
		printf("[%d] %s\n", h->len, h->str);
		h++;
	}
	tmp = h;
	return (tmp);
}
